import styled, { keyframes } from 'styled-components';

const header = styled.div`
  display: flex;
  font-size: 22px;
  padding-left: 29px;
  align-items: center;
`;

const bolder = styled.div`
  font-weight: bolder;
  font-size: 35px;
`;

const lighter = styled.div`
  font-weight: lighter;
  padding-left: 29px;
`;

export { header, bolder, lighter };
