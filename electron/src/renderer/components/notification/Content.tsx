/* eslint-disable @typescript-eslint/no-unused-vars */

import { useNavigate } from 'react-router-dom';
import TMail from '../notification/TMail';
function Content({ mail }: { mail: TMail }) {
  return (
  <>

  </>
  );
}

export default Content;
